name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [workflow_dispatch]
jobs:
  Explore-GitHub-Actions:
    runs-on: self-hosted
    # runs-on: ubuntu-latest
    steps:    
      - name: Check out repository code
        uses: actions/checkout@v4   
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver-opts: network=host
      - name: Build and push to local registry
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: localhost:5000/arndvorrath/githubactionsdemo:latest
      - name: Inspect
        run: |
          docker buildx imagetools inspect localhost:5000/arndvorrath/githubactionsdemo:latest   
   

  #SonarQubeScan-Job:
  #  runs-on: ubuntu-latest
  #  steps:
  #    - name: Check out repository code
  #      uses: actions/checkout@v4
  #    - name: SonarQube Scan
  #      uses: sonarsource/sonarqube-scan-action@master
  #      env:
  #        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }} //Sonar Token generated 
  #        SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }} example URL (https://sq.bswlog.co.in/)       
#
